<view class="s-list">
  <text class="title">{{ title }}</text>
  <text tt:if="{{ type === 'default' }}" class="value {{ color }}">{{
    value
  }}</text>
  <slot tt:elif="{{ type === 'slot' }}" class="value"></slot>
</view>
