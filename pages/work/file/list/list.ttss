.status {
  margin-top: 8px;
}

/* 作业文档列表 */
.item {
  margin-top: 12px;
}
.item:first-child {
  margin-top: 24px;
}

.title {
  margin-top: 24px;
  font-size: var(--font-size-h2);
  font-weight: var(--font-weight-h2);
  line-height: var(--line-height-h2);
}
