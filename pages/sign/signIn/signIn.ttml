<import src="/global/foot.ttml" />

<view class="page">
  <!-- 签到按钮 -->
  <text class="btn" bindtap="handleSignIn">{{_t['立即签到']}}</text>
  <view class="record width-app">
      <text class="title">{{_t['签到记录']}}</text>
      <view class="list">
        <s-list
          class="item"
          tt:if="{{ signList.length !== 0 }}"
          tt:for="{{ signList }}"
          title="{{ item.updateTime }}"
          action="{{ item.signedStatus ? _t['已签到'] : _t['未签到']}}"
        />
        <s-empty tt:else></s-empty>
      </view>
    </view>
</view>
<template is="foot"></template>
