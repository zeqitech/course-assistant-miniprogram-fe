<import src="/global/foot.ttml" />

<view class="page">
  <s-list
    tt:for="{{ docArray }}"
    class="item width-app animation-slide-bottom"
    style="animation-delay: {{(index+1)*0.1}}s;"
    icon="/image/doc.png"
    title="{{ item.fileName }}"
    action="{{ item.comment ? item.score : '打分' }}"
    color="{{ item.comment?(item.score<60?'red':'green'):'blue' }}"
    data-token="{{ item.fileToken }}"
    data-title="{{ item.fileName }}"
    bindtap="navToGrade"
  ></s-list>
</view>
<view class="popup">
  <text
    tt:for="{{ itemList }}"
    tt:if="{{ showPopup }}"
    data-index="{{ index }}"
    bindtap="handleMoreOption"
  >{{ item }}</text>
</view>
<image class="add-btn" src="/image/more.png" bindtap="handleTapMoreBtn"></image>
<template is="foot"></template>
